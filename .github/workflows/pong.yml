name: pong
on:
  workflow_run:
    workflows: [ping]
    types: [completed]
concurrency:
  group: chaintest-pong
jobs:
  go:
    if: github.run_number < 8
    runs-on: ubuntu-latest
    steps:
      - run: echo "PONG hop=${{ github.run_number }} cause=${{ github.event_name }}"
