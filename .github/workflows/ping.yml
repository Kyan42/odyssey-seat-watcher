name: ping
on:
  workflow_dispatch:
  workflow_run:
    workflows: [pong]
    types: [completed]
concurrency:
  group: chaintest-ping
jobs:
  go:
    if: github.run_number < 8
    runs-on: ubuntu-latest
    steps:
      - run: echo "PING hop=${{ github.run_number }} cause=${{ github.event_name }}"
